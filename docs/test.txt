================================================================================
MANUAL TEST PLAN — PowerSync Write API
================================================================================

Every claim the README and docs/ make, in an order that builds on itself. Work
top to bottom, or jump to a section — each one says what state it assumes.

Tick the [ ] as you go. Where a step has an expected result, it is marked with
"->". If something differs from the "->" line, that is the bug, whether the
fault is in the code or in the documentation.

This repo ships ONE service: the write API backend (`backend/`). It does not
bundle a database, a self-hosted PowerSync instance, or a runnable front-end —
`example-client/` is read-only reference code. Nothing in this plan requires a
real PowerSync Cloud/self-hosted deployment to be reachable: the demo verifier
only checks tokens against this backend's own JWKS, so the whole write path is
testable in isolation. A stand-in "adopter" Postgres, started outside Compose,
plays the role of "a database you already run."


--------------------------------------------------------------------------------
BEFORE YOU START
--------------------------------------------------------------------------------

[ ] Docker Desktop running.

[ ] Working from a clean checkout of this branch, at the repo root.

[ ] Nothing already on port 6060:

        lsof -nP -iTCP:6060 -sTCP:LISTEN

    -> no output. If a stray backend from a previous run is there, kill it.

[ ] Know the escape hatch. Any time you want to start over from nothing:

        docker compose down -v --remove-orphans
        docker rm -f adopter-db 2>/dev/null


--------------------------------------------------------------------------------
1. QUICKSTART — THE PATH IN THE README
--------------------------------------------------------------------------------

Assumes: nothing running, .env untouched from the checkout (DATABASE_URI blank,
POWERSYNC_URL/JWT_ISSUER blank, the committed throwaway signing keys in place).

[ ] Bring it up with no configuration at all:

        docker compose up --build

    -> the backend container exits, after up to 3 attempts (restart:
       on-failure:3), logging "Cannot start." and a message naming
       DATABASE_URI. This is the correct, documented failure — the README's
       Quickstart brings up the *container*, not a working server; the
       Configuration section below is what makes it boot. If the README reads
       to you like `docker compose up --build` alone should leave something
       listening on :6060, that is a documentation bug — file it.

[ ] Confirm nothing is listening:

        curl -s http://localhost:6060/

    -> connection refused.

[ ] Tear down:

        docker compose down


--------------------------------------------------------------------------------
2. STAND-IN "YOUR OWN DATABASE"
--------------------------------------------------------------------------------

Assumes: nothing running. This plays the part of the database the README says
you already have, so the rest of this plan has something real to write to.

[ ] Start a Postgres outside the project, on a different port:

        docker run -d --name adopter-db -p 5433:5432 \
          -e POSTGRES_PASSWORD=adopterpw -e POSTGRES_DB=adopter \
          postgres:18 -c wal_level=logical

[ ] Prepare it the way the README's table demands for Postgres — schema, then
    the publication that is easy to forget:

        docker exec adopter-db psql -U postgres -d adopter -c \
          "create table widgets (id uuid primary key default gen_random_uuid(), name text not null)"

        docker exec adopter-db psql -U postgres -d adopter -c \
          "create publication powersync for table widgets"

    -> both commands print CREATE TABLE / CREATE PUBLICATION.


--------------------------------------------------------------------------------
3. CONFIGURATION — MAKING IT BOOT
--------------------------------------------------------------------------------

Assumes: section 2 done, nothing from section 1 running.

[ ] Replace the throwaway signing keys:

        cd backend && pnpm generate-keys

    -> prints POWERSYNC_PRIVATE_KEY and POWERSYNC_PUBLIC_KEY values. Paste both
       into .env, replacing the committed pair.

[ ] Set the remaining values in .env:

        DATABASE_TYPE=postgres
        DATABASE_URI=postgres://postgres:adopterpw@host.docker.internal:5433/adopter
        POWERSYNC_URL=https://example.powersync.journeyapps.com
        JWT_ISSUER=https://example.powersync.journeyapps.com

    (host.docker.internal, not localhost — inside a container, localhost is
    the container. POWERSYNC_URL/JWT_ISSUER do not need to resolve to anything
    real for this plan — the backend only uses them as JWT claims and to
    verify its own tokens against its own key.)

[ ] Bring it up:

        docker compose up --build

    -> "Server is running @ http://127.0.0.1:6060", no restart loop.

[ ] Root route:

        curl -s http://localhost:6060/

    -> {"message":"backend"}

Leave this running for section 4.


--------------------------------------------------------------------------------
4. THE WRITE PATH, DIRECTLY
--------------------------------------------------------------------------------

Assumes: section 3 is up. This is how an adopter's own client actually drives
the backend, minus the PowerSync SDK in the middle.

[ ] Mint a token:

        TOKEN=$(curl -s "http://localhost:6060/api/auth/token?user_id=manual" \
          | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")
        echo "${TOKEN:0:24}..."

    -> a JWT prefix, not empty.

[ ] Check the key the backend verifies against is being served:

        curl -s http://localhost:6060/api/auth/keys

    -> a JWKS document with one key and a "kid".

[ ] Write a transaction batch, into the widgets table from section 2:

        curl -s -X POST http://localhost:6060/api/data \
          -H "Authorization: Bearer $TOKEN" \
          -H 'content-type: application/json' \
          -d '{"transactions":[{"transaction_id":1,"crud":[
                {"op":"PUT","table":"widgets","id":"11111111-1111-1111-1111-111111111111",
                 "op_data":{"name":"manual test row"}}]}]}'

    -> {"results":[{"status":"success"}]}

[ ] Prove it really reached the database, not just returned success:

        docker exec adopter-db psql -U postgres -d adopter -c \
          "select name from widgets;"

    -> "manual test row" is there.

[ ] Now the contract. Send a request that violates openapi.yaml:

        curl -s -X POST http://localhost:6060/api/data \
          -H "Authorization: Bearer $TOKEN" \
          -H 'content-type: application/json' \
          -d '{"transactions":"not-an-array"}'

    -> a 400 naming the offending field, from express-openapi-validator. Not a
       500, and not a success.

[ ] And auth. Same request, no token:

        curl -s -o /dev/null -w '%{http_code}\n' -X POST http://localhost:6060/api/data \
          -H 'content-type: application/json' -d '{"transactions":[]}'

    -> 401.

[ ] Confirm there is no authorization by default — every authenticated write
    is allowed, no matter what it touches (this is docs/authorization.md's
    claim). A second token for a different user_id can still write the same
    row:

        TOKEN2=$(curl -s "http://localhost:6060/api/auth/token?user_id=someone-else" \
          | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")
        curl -s -X POST http://localhost:6060/api/data \
          -H "Authorization: Bearer $TOKEN2" -H 'content-type: application/json' \
          -d '{"transactions":[{"transaction_id":1,"crud":[
                {"op":"DELETE","table":"widgets","id":"11111111-1111-1111-1111-111111111111"}]}]}'

    -> {"results":[{"status":"success"}]} — user_id "someone-else" was allowed
       to delete "manual"'s row. Confirm it is actually gone:

        docker exec adopter-db psql -U postgres -d adopter -c "select name from widgets;"

    -> empty.

[ ] Tear down, keeping the volume:

        docker compose down


--------------------------------------------------------------------------------
5. THE EXAMPLE CLIENT
--------------------------------------------------------------------------------

Assumes: nothing running. `example-client/` is read-only reference code, not a
runnable project — this section checks that claim and the ones around it.

[ ] Confirm it really has no build tooling of its own:

        ls example-client/package.json 2>/dev/null

    -> no such file.

[ ] Confirm the layout matches example-client/README.md's "What's here" table:

        find example-client/src -type f | sort

    -> generated/api.d.ts, library/powersync/{AppSchema,WriteAPIClient,
       OpenAPITransport,DemoConnector,DemoConnectorConfig,TransactionBatching}.ts

    (if DemoConnector.ts has been split further, the README's table and this
    list should still agree — that is the point of the check.)

[ ] Confirm DemoConnector.ts actually implements PowerSyncBackendConnector's
    two required methods, and that they are the first thing in the class body
    (the file's own "START HERE" comment claims this):

        grep -n "uploadData\|fetchCredentials\|class DemoConnector" \
          example-client/src/library/powersync/DemoConnector.ts | head -5

    -> uploadData and fetchCredentials both appear, ahead of the constructor.

[ ] Confirm example-client/.env.example's variables match the table in
    example-client/README.md — same names, same defaults:

        diff <(grep -oE '^VITE_[A-Z_]+' example-client/.env.example | sort) \
             <(grep -oE '`VITE_[A-Z_]+`' example-client/README.md | tr -d '`' | sort -u)

    -> no diff.


--------------------------------------------------------------------------------
6. CHANGING THE BACKEND CODE
--------------------------------------------------------------------------------

Assumes: nothing running, .env configured as in section 3. Two loops are
claimed — the fast one and the deployment-shaped one. Test both.

--- 6a. The development overlay (no rebuild) -----------------------------------

[ ] In .env (or exported in your shell), append the dev overlay:

        COMPOSE_FILE=docker-compose.yaml:docker-compose.dev.yaml

[ ] docker compose up --build   (the build is only needed the first time)

[ ] Confirm the backend is running under watch, not the built image:

        docker compose logs backend | tail -20

    -> tsx watch output, and "Server is running".

[ ] Edit backend/app.ts — change the root route's body:

        res.status(200).send({ message: 'backend' });
          ->
        res.status(200).send({ message: 'edited live' });

[ ] Save, wait ~2 seconds, then:

        curl -s http://localhost:6060/

    -> {"message":"edited live"} with NO rebuild and NO restart command.

[ ] Confirm node_modules were not clobbered by the bind mount (the macOS
    native-binary trap):

        docker compose exec backend node -e "require('pg'); console.log('deps ok')"

    -> deps ok. A crash here means the host's node_modules leaked in.

[ ] Revert the edit, confirm it reverts live.

[ ] docker compose down

--- 6b. The rebuild path -------------------------------------------------------

[ ] Unset COMPOSE_FILE / remove the dev overlay line, so plain
    docker-compose.yaml is in effect.

[ ] docker compose up -d --build   then   curl -s http://localhost:6060/

    -> {"message":"backend"} — the original, from the image.

[ ] Edit backend/app.ts again to 'rebuilt', then WITHOUT rebuilding:

        docker compose restart backend
        curl -s http://localhost:6060/

    -> still {"message":"backend"}. The image is what runs; a restart is not
       enough. This is the point of the distinction.

[ ] Now rebuild:

        docker compose up -d --build backend
        curl -s http://localhost:6060/

    -> {"message":"rebuilt"}.

[ ] Revert the edit and rebuild once more so the tree is clean.

[ ] docker compose down

--- 6c. Skipping Docker entirely (the root README's claim) ---------------------

[ ] cd backend && pnpm install && pnpm dev

    -> "Server is running @ http://127.0.0.1:6060" on the host, reading the
       root .env via dotenv, restarting on save.

[ ] Ctrl-C to stop.


--------------------------------------------------------------------------------
7. GENERATING TYPES FROM THE CONTRACT
--------------------------------------------------------------------------------

Assumes: nothing running.

[ ] Regenerate both stubs from openapi.yaml and confirm the committed ones are
    still current:

        cd backend && pnpm install && pnpm generate-types
        git status --short

    -> no diff on backend/src/generated/api.ts or
       example-client/src/generated/api.d.ts. If there is a diff, either
       openapi.yaml changed without regenerating, or generate-types silently
       drifted — both are findings.


--------------------------------------------------------------------------------
8. THE AUTOMATED SUITES
--------------------------------------------------------------------------------

Assumes: nothing running. Neither needs Docker or a real database — the tests
use their own fakes/mocks.

[ ] Backend test suite:

        cd backend && pnpm test

    -> all green.

[ ] Typecheck:

        cd backend && pnpm check

    -> no errors.

There is no root-level package.json or test suite — the repo root has nothing
to install and nothing to run (the README says this explicitly; confirm it's
still true):

        ls package.json 2>/dev/null

    -> no such file.


--------------------------------------------------------------------------------
9. FAILURE PATHS — THE MESSAGES SOMEONE WILL ACTUALLY HIT
--------------------------------------------------------------------------------

Assumes: .env configured as in section 3, nothing running. Each of these is a
mistake the docs promise to catch. If a message is unhelpful, that is a
finding.

[ ] Missing URI. Comment out DATABASE_URI in .env, then:

        docker compose up backend

    -> the backend exits, having printed a message that names DATABASE_URI and
       shows the URI shape. It must NOT loop forever: restart: on-failure:3
       gives up after 3 attempts so the message stays on screen. Restore
       DATABASE_URI afterwards.

[ ] Unsupported type. Restore DATABASE_URI, set DATABASE_TYPE=postgresql (the
    near-miss spelling PowerSync's own config uses), then:

        docker compose up backend

    -> a message saying postgresql is not supported and listing exactly:
       mongodb, mssql, mysql, postgres. Set it back to postgres afterwards.

[ ] Unstable signing keys. Blank POWERSYNC_PRIVATE_KEY and POWERSYNC_PUBLIC_KEY
    in .env, then:

        docker compose up -d
        docker compose logs backend | grep -i "temporary key pair"

    -> a warning that a temporary key pair was generated (the backend still
       starts — this is a soft fallback, not a boot failure). Now mint a
       token, restart, and use the OLD token:

        TOKEN=$(curl -s "http://localhost:6060/api/auth/token?user_id=manual" \
          | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")
        docker compose restart backend
        curl -s -X POST http://localhost:6060/api/data \
          -H "Authorization: Bearer $TOKEN" -H 'content-type: application/json' \
          -d '{"transactions":[]}'

    -> 401 — the restart generated a brand-new temporary key, so the old
       token's "kid" no longer matches anything in /api/auth/keys. Restore the
       committed (or your own regenerated) key pair in .env when done.

[ ] Port already in use. Start something on 6060 on the host, then
    `docker compose up`.

    -> a bind error naming 6060. (`cd backend && pnpm start` is the realistic
       way to cause this by accident.)


--------------------------------------------------------------------------------
TEARDOWN
--------------------------------------------------------------------------------

[ ] docker compose down -v --remove-orphans
[ ] docker rm -f adopter-db
[ ] git status --short    -> clean, apart from .env if you changed keys.


--------------------------------------------------------------------------------
NOTES / FINDINGS
--------------------------------------------------------------------------------

Section    What happened                                        Verdict
-------    -------------------------------------------------    -------



